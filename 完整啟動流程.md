# 完整啟動流程

## ✅ 已完成
- [x] Python 環境設置
- [x] 依賴套件安裝
- [x] Django 設置檢查
- [x] config 模組導入測試

## 📋 下一步操作

### 步驟 1: 建立 MySQL 資料庫（如果還沒建立）

**方法 A - MySQL 命令列：**
```bash
mysql -u root -p
```
輸入密碼後執行：
```sql
CREATE DATABASE issue_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

**方法 B - MySQL Workbench：**
1. 開啟 MySQL Workbench
2. 連接到 MySQL 伺服器
3. 執行：
```sql
CREATE DATABASE issue_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 步驟 2: 執行資料庫遷移（建表）

在 CMD 或 PowerShell 中（確保虛擬環境已啟動）：

```cmd
cd C:\Users\FAE\issue-system\backend
.venv\Scripts\activate.bat
python manage.py makemigrations
python manage.py migrate
```

或直接運行：`執行遷移_CMD.bat`

### 步驟 3: 建立超級使用者（選配，用於 Admin 後台）

```cmd
python manage.py createsuperuser
```

按照提示輸入：
- 使用者名稱（例如：admin）
- Email（可選，直接按 Enter 跳過）
- 密碼（輸入兩次）

### 步驟 4: 啟動開發伺服器

```cmd
python manage.py runserver 0.0.0.0:8000
```

或直接運行：`啟動伺服器_CMD.bat`

### 步驟 5: 驗證設置

開啟瀏覽器訪問：

1. **API 文件**: http://localhost:8000/api/docs/
   - 應該看到 Swagger UI 介面，可以測試 API

2. **Admin 後台**: http://localhost:8000/admin/
   - 使用步驟 3 建立的超級使用者登入
   - 可以管理資料

3. **API 測試**: http://localhost:8000/api/issues/
   - 應該看到空的 Issue 列表：`{"count":0,"results":[]}`

4. **字典表 API**: http://localhost:8000/api/settings/dictionaries/
   - 應該看到狀態、優先級、類別、來源等字典資料

## 🎉 完成！

後端 API 已成功啟動，現在可以：
1. 在前端連接後端 API
2. 使用 Admin 後台管理資料
3. 開始開發功能

## 🔄 以後使用

每次開啟終端後：

```cmd
cd C:\Users\FAE\issue-system\backend
.venv\Scripts\activate.bat
python manage.py runserver 0.0.0.0:8000
```

或直接運行：`啟動伺服器_CMD.bat`

